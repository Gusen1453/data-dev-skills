---
name: finance-data-qc
description: >-
  审查金融投研数据表的质量，产出质检报告（PASS/BLOCK/WATCH）与可定时重跑的监测脚本/SQL。当用户说
  "这些数据对不对 / 帮我质检(验收)一下XX表 / 出个数据质量报告 / 这个市值怎么这么离谱 / 主键重复了
  / 某天缺数据 / 财报怎么还没更新 / 和另一个库对不上 / 为什么捞出来一堆死票"时使用；也排查市值等
  数值离谱、主键重复、字段缺失、行情/财务序列断档、公告财报更新滞后、跨表单位命名时区不一致、
  注释口径含混、行业题材标签污染、查询接口返回截断或捞出失效标的。覆盖行情、财务、公告事件、
  证券主数据、指数基金、宏观行业等投研库。只读探索，结论全部可追溯、可重跑。不负责建表（用
  finance-ddl-design）、不负责管道（用 finance-pipeline-dev）、不负责查询接口设计（用
  finance-query-contract）。
---

# 金融投研数据质检

服务券商与基金的分析师、研究员和给他们供数的人。默认假设：**上游没有质量监控，字段注释不可信，测试库与生产库不一致**。你的任务不是相信数据，而是用最少的查询找出会让研究结论出错的地方。

## 这个 skill 交付什么

用户会指定一批表，让你产出两样东西：

1. **数据质量审查报告**（`report.md` + `evidence.json`）：逐项缺陷的观察、证据等级、PASS/BLOCK/WATCH 裁决、影响面与待补证据。
2. **监测脚本 / SQL**（`checks/`）：把本次发现的缺陷固化成可定时重跑的检查，交给用户落地为常驻监控（对应第 10 类缺陷“监控缺失”）。

审查报告是一次性结论，监测脚本是可复用资产。两者都必须真实可重跑，不能是自然语言描述。

## 怎么用这份 skill（路由总览）

SKILL.md 只给流程骨架和分发规则。具体方法在 `references/` 下，**按下面的触发条件按需加载，不要一次全读**。

严格按阶段推进，每个阶段读它该读的那一份：

| 阶段 | 动作 | 何时加载哪份 reference |
|---|---|---|
| 0 边界 | 确认表清单、用途、市场、实体、时间范围、交付要求 | 无需 reference |
| 1 引擎 | 识别每张表的存储引擎，定好只读、分页、成本估算方式 | **必读** `references/engine-adapters.md` |
| 2 探索 | 读元数据、估代价、规划分层采样 | **必读** `references/exploration.md` 与 `references/sampling-statistics.md` |
| 3 选场景 | 判断每张表属于哪个投研场景，取判定前提 | 按场景**至少读一份**下面的场景参考 |
| 4 选横切 | 判断是否触发跨表、时效、事故库 | 按触发条件读横切参考 |
| 5 筛缺陷 | 对十类缺陷逐类判适用性 | **必读** `references/defect-catalog.md` |
| 6 定检查 | 写 check 定义、check_id、预算、判据 | **必读** `references/deliverables.md` |
| 7 执行定级 | 跑检查、找反证、分级裁决 | 回到 defect-catalog / 场景参考取反证路径 |
| 8 交付 | 产出报告 + 监测脚本，按运行版本化 | `references/deliverables.md` |

### 场景参考（第 3 阶段，可多选，但要声明一个主场景）

| 表的内容 | 读哪份 |
|---|---|
| 行情报价、成交、交易日历、证券主数据、指数、基金净值、外汇、公司行动、复权 | `references/market-reference.md` |
| 财务报表与指标、业绩预告快报、估值指标、盈利预测、基金定期持仓披露 | `references/fundamental-valuation.md` |
| 交易所公告、招股与定期报告文本、研报、从文本抽取的结构化事件 | `references/disclosure-event.md` |
| 宏观经济指标、行业与产业链、行业分类、题材/概念到标的的映射 | `references/macro-industry.md` |

一张表跨多个场景时（如“财报公告表”“指数行业分类表”），读全部相关场景参考，但指定其中一个为主责任，其余作为补充判定前提。

### 横切参考（第 4 阶段，按触发条件加载）

| 触发条件 | 读哪份 |
|---|---|
| 用户指定了两张以上表，或需要 join、同义字段比对、测试库与生产库对照 | `references/cross-table-consistency.md` |
| 涉及缺失、连续性、最新数据、更新延迟、应到未到、SLA、点时可得（PIT） | `references/freshness.md` |
| 面对陌生库不知从何查起，或想核对常见坑 | `references/real-world-cases.md`（当作检查清单，已有明确缺陷时不必通读） |

单表、无时效诉求的简单验收可以不加载横切参考；不要为凑齐而读。

## 十类缺陷：逐类判适用性

第 5 阶段对每张表把这十类走一遍，但**只执行“适用且有判定前提”的检查**。逐类标记：`执行` / `不适用` / `前提不足暂挂`。不要对所有表机械跑满十类。检测手法、反证路径、“何时不能下结论”见 `references/defect-catalog.md`。

| 缺陷族 | 一句话 | 先看什么 |
|---|---|---|
| 主键重复 | 业务主键出现多行，其中一行信息残缺 | 声明主键的 count vs distinct，重复组内字段差异 |
| 缺失 | 原始表有值但下游字段空，或整类口径没建 | 分实体、分日期、分字段的空值率，与上游对照 |
| 连续性 | 序列断档，看起来"有数据"但月均只有几条 | 按实体×日历的记录数分布，最小/最大间隔 |
| 数值错误 | 量级离谱、单位错、符号错、跨字段矛盾 | 极值、量级分位、跨字段可复算关系 |
| 时效 | 应到未到，或到得比承诺晚 | 最后更新时间、到达延迟分布、批次节奏 |
| 命名与单位 | 同义字段跨表不同名、不同单位、不同时区 | 同义字段清单，量级比对，时区抽样 |
| 注释口径 | 注释能被读成两种意思，或与实际计算不符 | 注释措辞歧义，用数据反推真实口径 |
| 维度污染 | 分类字段混入其他体系的标签，一统计就翻倍 | 枚举值取值域，分组求和与总量对账 |
| 接口层 | 传参 20 返 少、失效标的被捞出、查询超时 | 请求数与返回数对账，边界与分页，慢查询 |
| 监控缺失 | 上述缺陷没有常驻校验，靠人肉发现 | 有没有落地的持续校验与告警 |

## 定级只认三种证据

- **查到的（OBSERVED）**：查询、文件、元数据实际返回什么。
- **推断的（INFERRED）**：字段含义、异常原因、规则是否适用的假设。
- **确认的（CONFIRMED）**：带版本的规范或数据契约、用户对该范围的明确确认、用独立数据源或业务关系复算成功。

定级规则：

- 只有 `CONFIRMED` 口径才能给 `PASS` 或 `BLOCK`，且必须能追溯到可重跑的检查和同一数据范围。
- 只凭字段名、注释或数值分布得出的结论只能给 `WATCH`，不得直接判 `BLOCK`。
- 检查没跑、跑不完或预算不够，写明停止原因。**不要把"没发现问题"写成 PASS。**

## 开工前先落四件事

1. **只读。** 只用只读账号和只读语句。禁止 DDL/DML，禁止无界 `SELECT *`。
2. **范围。** 记下用户指定的表清单、市场、标的类型、实体集合、时间范围、频率、这批数据要拿去做什么（选股、回测、估值、报告取数）。
3. **查询预算。** 先声明行数、扫描量、执行时间或 API 次数上限，再发第一条查询。每轮查询写明目的、投影列、过滤条件、上限。
4. **未知就写未知。** 不清楚的口径列进未知清单，不要用字段名猜、不要拿其他市场的阈值套。

## 执行流程

1. **确认边界。** 逐表登记用途、市场、标的类型、实体范围、时间范围，以及用户要的交付物（报告、监测脚本或两者）。
2. **认引擎。** 先判定每张表的存储引擎（关系库、数仓、ES、Mongo、时序库、向量库、文件、API），据此定只读方式、分页、采样与成本估算。见 `references/engine-adapters.md`。这一步必须在第一条业务查询之前完成。
3. **摸结构、估代价。** 读元数据：字段类型、分区、索引、声明约束、行数量级；用执行计划或引擎等价能力确认不会扫全表。见 `references/exploration.md`。
4. **漏斗式采样统计。** 按“元数据 → 小范围侦察 → 分层统计 → 异常下钻 → 有理由地扩大”推进。`LIMIT` 不等于限制扫描；不得一上来跑全历史 `COUNT DISTINCT`、高基数 `GROUP BY` 或大表 join。见 `references/sampling-statistics.md`。
5. **选参考。** 按上面的场景表与横切表加载对应 reference，取判定前提，不照抄阈值。
6. **筛缺陷。** 对十类逐类判适用性，只保留适用且有前提的检查。见 `references/defect-catalog.md`。
7. **定检查。** 先读 `references/deliverables.md` 定好检查文件与 `check_id` 规范，再为每条检查写清适用条件、数据范围、预算、PASS/BLOCK/WATCH 判据。
8. **执行与反证。** 优先用能形成闭环的关系复算（价格×数量、分组求和对总量、分钟重算日频、上游原始表对下游宽表），而不是继续堆分布统计。
9. **定级。** 逐项给证据等级与 PASS/BLOCK/WATCH，写清影响面与待补证据。
10. **交付。** 产出审查报告与监测脚本，按运行版本化保存，历史运行不许覆盖。见下节与 `references/deliverables.md`。

正反例见 `examples/few-shots.md`。

## 交付物要求

- **审查报告** `report.md`：数据范围与快照、查询预算与实际消耗、逐项 `check_id` 的观察/证据等级/裁决、`INFERRED` 假设的依据与反证条件、时效与覆盖观察、与上次运行的差异。
- **机器可读证据** `evidence.json`：每条检查的可重跑文件引用、参数、观察结果、证据等级、裁决。
- **监测脚本 / SQL** `checks/`：每条固化为该引擎原生可执行格式（SQL 用 `.sql`，ES/Mongo/时序/向量用其原生请求，Python 用 `.py`），带稳定 `check_id`、参数化数据范围、判据与告警阈值建议，供用户直接接入调度。不得把非 SQL 检查伪装成 `.sql`。
- 监测脚本要能独立于本次会话运行：参数化时间窗与实体范围，不硬编码一次性快照。

## 必须停下的情况

- 连接信息里有明文口令、私钥或会被写进报告的令牌。
- 只能拿到有写权限的账号，或任务要求执行 DDL/DML。
- 查询无法限定投影、实体、时间、分区或结果上限，可能形成无界扫描。
- 已用或预估扫描量超出批准的查询预算。
- 样本会暴露未脱敏的账户、持仓、交易或客户信息。
- 数据是压缩、加密、专有二进制或流式形态，没有安全的只读解析方式。
- 任务实质是要 DDL 设计、管道编排、查询接口设计、治理或合规的最终结论：先保存已有观察，再回到 `using-data-dev` 选对应责任。
